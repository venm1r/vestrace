# Vestrace P0 Restore Main — Exit Gate Report

**Baseline:** `531bf73b9e3850de7fb09949647f6da402d1a467`  
**Implementation branch:** `agent/p0-restore-main`  
**Primary verification:** GitHub Actions run `30950915993`  
**Status:** Passed

## Outcome

P0 removed the confirmed false-success surfaces from the baseline without importing P1 State Engine work or expanding the product scope.

The supported runtime remains intentionally narrow, but every exposed operational surface now either performs real work or fails explicitly.

## Changes verified

- `vestrace worker` exits non-zero with an explicit not-implemented error;
- `vestrace migrate` connects to PostgreSQL, applies the embedded SQLx migrator, and verifies exact history compatibility;
- migration execution and readiness share one embedded migrator and one compatibility algorithm;
- server startup verifies migration compatibility before serving;
- database connection and migration failures are mapped to sanitized errors;
- architecture and quickstart documentation match the actual workspace and CLI behavior;
- CI rejects stale false-success messages and stale migration-range claims;
- CI rejects direct SQLx or infrastructure dependencies in HTTP source;
- CI builds the real CLI, applies migrations twice, and verifies explicit command failures;
- clean-volume Compose acceptance remains green.

## TDD evidence

### Initial CLI RED

The first binary contract run failed exactly two tests:

- `worker_fails_explicitly_instead_of_reporting_false_success`;
- `migrate_fails_when_database_is_unavailable_and_redacts_secrets`.

All pre-existing tests passed.

### Migration API RED

The migration-operation contract failed because `PgStore::migrations_are_compatible` did not exist. This established the requirement for one public infrastructure-level compatibility operation shared by readiness and operational commands.

### GREEN

Run `30950915993` passed:

- Rust formatting;
- documentation truthfulness gate;
- HTTP boundary gate;
- Clippy across all workspace targets and features;
- full Rust/PostgreSQL test suite;
- CLI package build;
- standalone migration against a clean database;
- second idempotent migration run;
- CLI explicit-failure and secret-redaction acceptance;
- console install, typecheck, and production build;
- Docker Compose build on a clean project/volume;
- liveness and readiness smoke;
- run-record create/list/get and workspace isolation;
- restricted runtime RLS acceptance;
- volume cleanup.

## Exit gate

| # | Condition | Result | Evidence |
|---:|---|---|---|
| 1 | Workspace compiles | Pass | Rust suite and Clippy |
| 2 | `cargo fmt --all --check` passes | Pass | lint job |
| 3 | Clippy passes with `-D warnings` | Pass | lint job |
| 4 | Full Rust tests pass | Pass | tests job |
| 5 | Console typecheck/build pass | Pass | console job |
| 6 | Compose starts from a clean volume | Pass | compose-acceptance job |
| 7 | HTTP source has no direct SQLx/infrastructure dependency | Pass | boundary truth gate |
| 8 | Embedded migrations initialize a clean database | Pass | migration operation test and CLI migration step |
| 9 | Migration execution is idempotent | Pass | repeated infrastructure and CLI migration checks |
| 10 | Readiness uses exact embedded-history compatibility | Pass | shared `PgStore` method and health tests |
| 11 | `worker` cannot report false success | Pass | binary and shell acceptance |
| 12 | `migrate` cannot report false success | Pass | unavailable/clean database acceptance |
| 13 | Placeholder commands exit non-zero | Pass | CLI truthfulness checks |
| 14 | CLI errors do not reveal database credentials | Pass | binary and shell redaction assertions |
| 15 | Unsupported HTTP surfaces remain explicit | Pass | existing router contracts and Compose smoke |
| 16 | Documentation matches six-crate workspace | Pass | architecture update and doc gate |
| 17 | Documentation has no authoritative migration maximum | Pass | architecture update and doc gate |
| 18 | Console does not substitute mock success | Pass | existing console contract plus typecheck/build |
| 19 | Runtime workspace isolation remains enforced | Pass | HTTP isolation and RLS acceptance |
| 20 | No P1 runtime scope entered P0 | Pass | branch diff review |

## Diff boundary

The branch contains only P0 CLI, migration, documentation, tests, scripts, and CI changes. It does not include code from draft PRs `#22`–`#25`, and it does not add State Engine, agents, workflows, memory, artifact, capability, or UI functionality.

## Next phase

After this P0 branch is reviewed and integrated, P1 begins with the State Engine vertical in this order:

1. typed event-sourced core;
2. checkpoints, replay, and projection rebuild;
3. pause, resume, and retries;
4. persisted command idempotency where required by command boundaries;
5. delegated subagents;
6. compensation.
