# MW-07. Full-workflow feature qualification

<!-- Generated by docs/maintenance/render_catalogs.py; edit traceability.json and task-details.json. -->

**Status:** Proposed implementation plan. **Dependencies:** MW-01–MW-06.

The [shared scope, task procedure, and exit gate](README.md) apply to every task. Read [design](../01-design.md), [transactions](../02-data-and-transactions.md), [API](../03-api.md), and [acceptance](../09-acceptance.md). These instructions record no runtime results.

## Package file map

Original statuses are relative to the original reviewed baseline. Reread existing paths and check new names against the implementation checkout. This map is not blanket write authority.

| Path | Original planning status | Responsibility |
| --- | --- | --- |
| `docker/postgres/init-runtime-role.sh` | `existing_referenced_recheck_before_edit` | Exact guarded ownership/execute allowlists |
| `.github/workflows/ci.yml` | `existing_referenced_recheck_before_edit` | Proposed memory CI coverage including docs and browser gates |
| `tests/memory_workspace_acceptance.rs` | `proposed_new_check_name_collisions` | Planned verification target, not an existing passed suite |
| `crates/vestrace-infrastructure/tests/memory_workspace_upgrade.rs` | `proposed_new_check_name_collisions` | Planned verification target, not an existing passed suite |
| `scripts/memory-workspace-acceptance.sh` | `proposed_new_check_name_collisions` | Planned verification target, not an existing passed suite |

## T07.1. Clean-install and populated-upgrade qualification

**Requirements:** MW-R39.  
**Acceptance:** A47, A48.

### Exact task files

- `crates/vestrace-infrastructure/tests/memory_workspace_upgrade.rs`
- `docker/postgres/init-runtime-role.sh`
- `tests/memory_workspace_acceptance.rs`

### Interfaces, implementation, and required observations

**Interface:** Consume logical M01–M04, existing provisioner/schema gates, and ordinary-material
requirements. Produce restricted-runtime clean-install/upgrade/refusal observations—not a
new repair authority.

Prepare a disposable clean database and a populated baseline via real old interfaces. Apply
forward migrations and exact roles; compare historical data and legacy receipts. Attempt
raw DML on every new guarded table and cross-workspace reference and assert the intended
refusal mechanism. Test ordinary-material recovery/erasure with source/export refs and no
cross-source cascade leakage. Existing migration checksums and protocol locks remain unchanged
outside approved new scope.

A47/A48 require both clean and populated upgrade under the limited runtime role. A privileged
fixture cannot replace those checks or conceal missing grants.

### Verification command

Run only after the target/fixture exists. Verify behavioral RED, then GREEN and every linked case under the [shared procedure](README.md#shared-task-procedure). These are not results of this documentation edition.

```bash
cargo test -p vestrace-infrastructure --test memory_workspace_upgrade --locked -- --test-threads=1
```


## T07.2. Complete user workflow and final review

**Requirements:** MW-R40, MW-R41, MW-R43, MW-R44, MW-R45.  
**Acceptance:** A49, A50, A52, A53, A54.

### Exact task files

- `tests/memory_workspace_acceptance.rs`
- `scripts/memory-workspace-acceptance.sh`
- `.github/workflows/ci.yml`

### Interfaces, implementation, and required observations

**Interface:** Consume actual built binary, HTTP, worker, Console, registered topics, scoped
fixtures, and the complete A01–A54 matrix. Produce a versioned evidence manifest with source,
build, environment, exact commands, and limitations. MW completion does not set TRUSTED.

Run upload → read → edit → sync conflict → resolve → export → new target with production
producers. Kill actual child processes after business commit/before acknowledgement and
during linking; inspect durable outcomes through a fresh reader. Compare served OpenAPI,
actual JSON, and SDK fields. Run doctests and browser checks explicitly. Mutate receipt/CAS/
workspace/atomicity/override/download-policy guards in disposable copies, restore, and rerun.
Independent final review reads the whole contract and current tree; list unresolved material,
generation, migration, or environment blockers. Qualify only the approved supported scenario,
not full v1.0 or P04 closure.

Run all linked A49/A50/A52/A53/A54 and complete relevant suites. Syntax/typecheck alone is
never feature acceptance.

### Verification command

Run only after the target/fixture exists. Verify behavioral RED, then GREEN and every linked case under the [shared procedure](README.md#shared-task-procedure). These are not results of this documentation edition.

```bash
bash scripts/memory-workspace-acceptance.sh
cargo test --workspace --doc --all-features --locked
npm --prefix apps/console run test:e2e:memory
```

## Exit gate

Apply the [shared exit gate](README.md#shared-exit-gate). Unavailable prerequisites remain explicit blockers; a checkbox or document check is not runtime evidence.
