# MW-01. Memory reads and Context API

<!-- Generated by docs/maintenance/render_catalogs.py; edit traceability.json and task-details.json. -->

**Status:** Proposed implementation plan. **Dependencies:** MW-00.

The [shared scope, task procedure, and exit gate](README.md) apply to every task. Read [design](../01-design.md), [transactions](../02-data-and-transactions.md), [API](../03-api.md), and [acceptance](../09-acceptance.md). These instructions record no runtime results.

## Package file map

Original statuses are relative to the original reviewed baseline. Reread existing paths and check new names against the implementation checkout. This map is not blanket write authority.

| Path | Original planning status | Responsibility |
| --- | --- | --- |
| `crates/vestrace-application/src/memory/mod.rs` | `existing_inspected` | Shared memory use cases and exports for new query/write ports. |
| `crates/vestrace-http/src/api/memory.rs` | `existing_inspected` | Preserve legacy wire shapes and connect the shared correction writer. |
| `crates/vestrace-http/src/api/retrieval.rs` | `existing_inspected` | Preserve legacy retrieval; reuse the new shared context service. |
| `crates/vestrace-application/src/retrieval/context_builder.rs` | `existing_inspected` | Actual sections, exact final rendering, and counter qualification. |
| `crates/vestrace-application/src/lib.rs` | `existing_referenced_recheck_before_edit` | Connect new modules with narrow exports. |
| `crates/vestrace-infrastructure/src/postgres/mod.rs` | `existing_referenced_recheck_before_edit` | Export PostgreSQL adapters. |
| `crates/vestrace-http/src/api/mod.rs` | `existing_referenced_recheck_before_edit` | Mount new endpoints through the route inventory. |
| `crates/vestrace-http/src/router.rs` | `existing_referenced_recheck_before_edit` | Wire AppState dependencies rather than default-success placeholders. |
| `crates/vestrace-http/src/route_inventory.rs` | `existing_referenced_recheck_before_edit` | Every new route capability/risk and negative coverage |
| `crates/vestrace-cli/src/commands/server.rs` | `existing_referenced_recheck_before_edit` | Production read/write/import/export composition |
| `crates/vestrace-cli/src/commands/mcp.rs` | `existing_referenced_recheck_before_edit` | Shared read service composition |
| `crates/vestrace-mcp/src/server.rs` | `existing_referenced_recheck_before_edit` | Preserve tool names; lawful content hydration |
| `crates/vestrace-cli/src/commands/schema.rs` | `existing_referenced_recheck_before_edit` | Served OpenAPI generator; do not replace whole schema |
| `crates/vestrace-application/src/memory/read.rs` | `proposed_new_check_name_collisions` | MemoryReadService and lawful view ports |
| `crates/vestrace-application/src/memory/context_delivery.rs` | `proposed_new_check_name_collisions` | RenderedMemoryContext, renderer and token-counter port |
| `crates/vestrace-infrastructure/src/postgres/memory_read_repository.rs` | `proposed_new_check_name_collisions` | Consistent detail/history/browse queries |
| `crates/vestrace-http/src/api/memory_workspace.rs` | `proposed_new_check_name_collisions` | New detail/history/correction routes and DTO conversion |
| `crates/vestrace-http/src/api/context_packs.rs` | `proposed_new_check_name_collisions` | ContextRequest/ContextResponse endpoint |
| `crates/vestrace-infrastructure/tests/memory_read_contract.rs` | `proposed_new_check_name_collisions` | Planned verification target, not an existing passed suite |
| `crates/vestrace-http/tests/memory_workspace_api.rs` | `proposed_new_check_name_collisions` | Planned verification target, not an existing passed suite |
| `crates/vestrace-application/tests/context_delivery.rs` | `proposed_new_check_name_collisions` | Planned verification target, not an existing passed suite |
| `crates/vestrace-cli/tests/memory_read_wiring.rs` | `proposed_new_check_name_collisions` | Planned verification target, not an existing passed suite |
| `migrations/0196_memory_workspace_receipts.sql` | `candidate_number_not_reserved` | Candidate M01 in MW-01: transactional epoch triggers, exact links and receipt schema; MW-02 reads/uses, never rewrites applied SQL |

## T01.1. Shared authorized content reads

**Requirements:** MW-R02, MW-R07.  
**Acceptance:** A02, A09.

### Exact task files

- `crates/vestrace-application/src/memory/read.rs`
- `crates/vestrace-infrastructure/src/postgres/memory_read_repository.rs`
- `crates/vestrace-http/src/api/memory_workspace.rs`
- `crates/vestrace-cli/tests/memory_read_wiring.rs`

### Interfaces, implementation, and required observations

**Interface:** Proposed MemoryReadRepository::detail(&RequestContext, MemoryId) returns
Result<Option<MemoryDetail>, ApplicationError>; revision(&RequestContext, MemoryId,
MemoryRevisionId) returns Result<Option<Revision>, ApplicationError>. MemoryReadService
applies existing capability/content/disclosure policy before DTO conversion. Keep application
results separate from HTTP serialization and reuse RequestContext/Memory/MemoryRevision.

Open one consistent scoped read for memory, active revision, and permitted origins. Validate
revision/workspace identity and current content rights. Return only permitted fields;
unknown/denied objects share 404. Wire actual server and MCP composition rather than leaving
an unnoticed default-unavailable adapter. Preserve the old metadata GET. MW-02 supplies
exact new provenance links; legacy associations remain legacy_unattributed.

A02/A09 require real server/MCP composition and refusal without source metadata leakage;
a direct service-object test alone is insufficient.

### Verification command

Run only after the target/fixture exists. Verify behavioral RED, then GREEN and every linked case under the [shared procedure](README.md#shared-task-procedure). These are not results of this documentation edition.

```bash
cargo test -p vestrace-infrastructure --test memory_read_contract --locked
cargo test -p vestrace-cli --test memory_read_wiring --locked
```


## T01.2. Consistent browse and history pagination

**Requirements:** MW-R03.  
**Acceptance:** A03, A04.

### Exact task files

- `crates/vestrace-application/src/memory/read.rs`
- `crates/vestrace-infrastructure/src/postgres/memory_read_repository.rs`
- `crates/vestrace-http/tests/memory_workspace_api.rs`
- `migrations/0196_memory_workspace_receipts.sql`
- `docker/postgres/init-runtime-role.sh`

### Interfaces, implementation, and required observations

**Interface:** Proposed MemoryReadRepository::list(&RequestContext, BrowseRequest) returns
MemoryPage; history(&RequestContext, MemoryId, HistoryRequest) returns RevisionPage, both
through Result<_, ApplicationError>. BrowseRequest includes kind/status/collection filters,
limit, and opaque cursor; HistoryRequest includes limit/cursor. Follow chapter 02.6.
The collection filter is unavailable until MW-04 supplies its lawful relationship.

Apply disclosure predicates before keyset (created_at,id) selection with limit N+1. Bind
cursors to scope/filter/epoch/policy without hidden IDs. History filters inaccessible content
and pins the highest visible revision on page one. MW-01 applies M01 schema and transactional
epoch triggers covering existing writers. Test read consistency now rather than defer it to
MW-02; the correction receipt schema exists, but its new writer is enabled in MW-02.

A03/A04 cover equal timestamps, concurrent mutation, explicit cursor expiry, and direct
historical URL authorization. No silently duplicated/omitted records.

### Verification command

Run only after the target/fixture exists. Verify behavioral RED, then GREEN and every linked case under the [shared procedure](README.md#shared-task-procedure). These are not results of this documentation edition.

```bash
cargo test -p vestrace-infrastructure --test memory_read_contract --locked
cargo test -p vestrace-http --test memory_workspace_api --locked
```


## T01.3. Useful context and accurate size accounting

**Requirements:** MW-R05, MW-R06.  
**Acceptance:** A06, A07, A08.

### Exact task files

- `crates/vestrace-application/src/memory/context_delivery.rs`
- `crates/vestrace-application/src/retrieval/context_builder.rs`
- `crates/vestrace-http/src/api/context_packs.rs`
- `crates/vestrace-application/tests/context_delivery.rs`

### Interfaces, implementation, and required observations

**Interface:** Proposed ContextDeliveryService::build(&RequestContext, ContextRequest) returns
Result<ContextResponse, ApplicationError>. QualifiedTextCounter::count(&str) returns
Result<u32, ApplicationError>, with identity() -> &str and registration bound to an approved
model/destination. No default counter promises an arbitrary-model ceiling. Consume existing
RetrievalService/ContextPack items and output the exact sections/rendered_context schema.
Byte-only output is not normative ContextPack2 qualification.

Use existing generation/policy guards and hydrate only authorized exact revisions. Render
real content and retain SourceRef; omit absent text instead of substituting explanation.
Build one deterministic final string with citations/separators and enforce its complete
UTF-8 cap. Qualified token mode counts that final string; missing counters return 422.
Recheck policy/generation before disclosure: one bounded stale retry without a new external
dispatch, otherwise a typed error.

Run A06–A08 and cross-cutting A09/A52. Estimates cannot silently become strict guarantees.

### Verification command

Run only after the target/fixture exists. Verify behavioral RED, then GREEN and every linked case under the [shared procedure](README.md#shared-task-procedure). These are not results of this documentation edition.

```bash
cargo test -p vestrace-application --test context_delivery --locked
cargo test -p vestrace-http --test memory_workspace_api --locked
```

## Exit gate

Apply the [shared exit gate](README.md#shared-exit-gate). Unavailable prerequisites remain explicit blockers; a checkbox or document check is not runtime evidence.
