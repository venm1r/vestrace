# MW-02. Atomic corrections and replay

<!-- Generated by docs/maintenance/render_catalogs.py; edit traceability.json and task-details.json. -->

**Status:** Proposed implementation plan. **Dependencies:** MW-00, MW-01.

The [shared scope, task procedure, and exit gate](README.md) apply to every task. Read [design](../01-design.md), [transactions](../02-data-and-transactions.md), [API](../03-api.md), and [acceptance](../09-acceptance.md). These instructions record no runtime results.

## Package file map

Original statuses are relative to the original reviewed baseline. Reread existing paths and check new names against the implementation checkout. This map is not blanket write authority.

| Path | Original planning status | Responsibility |
| --- | --- | --- |
| `crates/vestrace-application/src/memory/mod.rs` | `existing_inspected` | Shared memory use cases and exports for new query/write ports. |
| `crates/vestrace-application/src/memory/ports.rs` | `existing_inspected` | MemoryRepository compatibility and the caller-owned write contract. |
| `crates/vestrace-application/src/memory/services.rs` | `existing_inspected` | Move affected canonical mutations onto the shared atomic writer. |
| `crates/vestrace-infrastructure/src/postgres/memory_repository.rs` | `existing_inspected` | Existing CAS and writes in a UoW; no nested independent commit. |
| `crates/vestrace-application/src/governed_mutation.rs` | `existing_inspected` | Reuse commit_in without introducing another authority. |
| `crates/vestrace-application/src/idempotency.rs` | `existing_inspected` | Compatibility existing scoped receipts |
| `crates/vestrace-http/src/api/memory.rs` | `existing_inspected` | Preserve legacy wire shapes and connect the shared correction writer. |
| `crates/vestrace-application/src/lib.rs` | `existing_referenced_recheck_before_edit` | Connect new modules with narrow exports. |
| `crates/vestrace-infrastructure/src/postgres/mod.rs` | `existing_referenced_recheck_before_edit` | Export PostgreSQL adapters. |
| `crates/vestrace-infrastructure/src/postgres/pool.rs` | `existing_referenced_recheck_before_edit` | Inspect existing permits/UoW; edit only when necessity is demonstrated. |
| `crates/vestrace-http/src/api/mod.rs` | `existing_referenced_recheck_before_edit` | Mount new endpoints through the route inventory. |
| `crates/vestrace-http/src/router.rs` | `existing_referenced_recheck_before_edit` | Wire AppState dependencies rather than default-success placeholders. |
| `crates/vestrace-http/src/route_inventory.rs` | `existing_referenced_recheck_before_edit` | Every new route capability/risk and negative coverage |
| `crates/vestrace-cli/src/commands/server.rs` | `existing_referenced_recheck_before_edit` | Production read/write/import/export composition |
| `crates/vestrace-cli/src/commands/schema.rs` | `existing_referenced_recheck_before_edit` | Served OpenAPI generator; do not replace whole schema |
| `docker/postgres/init-runtime-role.sh` | `existing_referenced_recheck_before_edit` | Exact guarded ownership/execute allowlists |
| `crates/vestrace-application/src/memory/write.rs` | `proposed_new_check_name_collisions` | MemoryMutationPlan/receipt writer over shared UoW |
| `crates/vestrace-infrastructure/src/postgres/memory_mutation_repository.rs` | `proposed_new_check_name_collisions` | Caller-owned atomic correction and import participant |
| `crates/vestrace-http/src/api/memory_workspace.rs` | `proposed_new_check_name_collisions` | New detail/history/correction routes and DTO conversion |
| `crates/vestrace-infrastructure/tests/memory_mutation_atomicity.rs` | `proposed_new_check_name_collisions` | Planned verification target, not an existing passed suite |
| `crates/vestrace-infrastructure/tests/memory_mutation_replay.rs` | `proposed_new_check_name_collisions` | Planned verification target, not an existing passed suite |
| `crates/vestrace-http/tests/memory_correction_api.rs` | `proposed_new_check_name_collisions` | Planned verification target, not an existing passed suite |

## T02.1. Shared transaction participant for memory mutation

**Requirements:** MW-R04, MW-R08.  
**Acceptance:** A05, A10.

### Exact task files

- `crates/vestrace-application/src/memory/write.rs`
- `crates/vestrace-infrastructure/src/postgres/memory_mutation_repository.rs`
- `crates/vestrace-infrastructure/src/postgres/memory_repository.rs`
- `crates/vestrace-infrastructure/tests/memory_mutation_atomicity.rs`

### Interfaces, implementation, and required observations

**Interface:** MemoryMutationPlan carries authenticated context, target/base content and state
revisions, action payload, reason, exact source link, and semantic key. Proposed
MemoryMutationRepository::apply_in(&RequestContext, &mut dyn UnitOfWork, &MemoryMutationPlan)
returns Result<MutationReceipt, ApplicationError>. Reuse InstallationMutationPermit and
GovernedMutationRepository::commit_in. Result IDs become authoritative at caller commit;
no child method commits independently. Applied M01 is a dependency, not a writable task file.

Take approved guards and existing database CAS with bounded revision conversion. Write
capture/correction Event, MemoryRevision, MemorySource, exact link, and storage projection
in the same UoW. Shared governed mutation includes audit/outbox/receipt. The database trigger
advances epoch once; do not repeat it in application code. No provider/vault/network work
runs inside the transaction. Do not edit the M01 checksum or infer old attribution.

A05/A10 verify exact new provenance and rollback of revision/audit/outbox/receipt/epoch,
including active-head integrity.

### Verification command

Run only after the target/fixture exists. Verify behavioral RED, then GREEN and every linked case under the [shared procedure](README.md#shared-task-procedure). These are not results of this documentation edition.

```bash
cargo test -p vestrace-infrastructure --test memory_mutation_atomicity --locked
```


## T02.2. Durable replay and conflicting intent

**Requirements:** MW-R09, MW-R10.  
**Acceptance:** A11, A12, A13.

### Exact task files

- `crates/vestrace-application/src/memory/write.rs`
- `crates/vestrace-infrastructure/src/postgres/memory_mutation_repository.rs`
- `crates/vestrace-application/src/idempotency.rs`
- `crates/vestrace-infrastructure/tests/memory_mutation_replay.rs`

### Interfaces, implementation, and required observations

**Interface:** Proposed MemoryMutationRepository::commit(&RequestContext, MemoryMutationPlan)
returns Result<MutationReceipt, ApplicationError>, coordinating the existing UoW and apply_in.
Use chapter 02.5 key scope, normalized input, and approved keyed commitments, not generated
result IDs. Store the receipt schema without plaintext bodies.

Reauthorize and lock the existing canonical key. Exact committed replay returns the original
receipt before stale CAS and never invokes Apply again. Changed intent returns 409; pending
contenders wait boundedly or receive busy. Only the no-receipt path applies and commits.
Replay lost responses from durable state. Exercise independent connections and actual child
process death after commit/before response. Removing the winner check must fail the unchanged test.

A11–A13 require one effect, identical winner receipt, changed-body refusal, and no receipt
access for an unauthorized principal.

### Verification command

Run only after the target/fixture exists. Verify behavioral RED, then GREEN and every linked case under the [shared procedure](README.md#shared-task-procedure). These are not results of this documentation edition.

```bash
cargo test -p vestrace-infrastructure --test memory_mutation_replay --locked -- --test-threads=1
```


## T02.3. Correction/restore and complete legacy adaptation

**Requirements:** MW-R11, MW-R12.  
**Acceptance:** A14, A15, A16.

### Exact task files

- `crates/vestrace-http/src/api/memory_workspace.rs`
- `crates/vestrace-http/src/api/memory.rs`
- `crates/vestrace-application/src/memory/services.rs`
- `crates/vestrace-http/tests/memory_correction_api.rs`

### Interfaces, implementation, and required observations

**Interface:** POST corrections accepts closed CorrectionRequest with a required UUID key,
returning 201 MutationReceipt. action=restore reads old content under current policy and
creates a new revision. Preserve legacy remember/revise signatures and MemoryResponse;
adapt both to the shared participant. Inherit labels, without a new classification write field.

Validate closed bodies. Preserve legacy numeric If-Match; new corrections use explicit body
preconditions only. Refuse restore when target classification would weaken source obligations.
Execute the common writer with 409 on basis mismatch. Do not infer extra lifecycle actions.
Update route inventory, schema generator, and production composition together. Replay precedes
CAS through the same writer. Run legacy regressions alongside the new endpoint suite.

A14–A16 require concurrent-edit protection, restored text in a new r5 with history preserved,
and no remaining legacy non-atomic bypass.

### Verification command

Run only after the target/fixture exists. Verify behavioral RED, then GREEN and every linked case under the [shared procedure](README.md#shared-task-procedure). These are not results of this documentation edition.

```bash
cargo test -p vestrace-http --test memory_correction_api --locked
cargo test -p vestrace-infrastructure --test memory_classification --locked
```

## Exit gate

Apply the [shared exit gate](README.md#shared-exit-gate). Unavailable prerequisites remain explicit blockers; a checkbox or document check is not runtime evidence.
